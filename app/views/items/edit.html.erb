<% if flash[:errors] %>
  <%= pluralize(flash[:errors].count, "error") %> prevented this item from being edited:
  <ul>
    <% flash[:errors].each do |e| %>
      <li><%= e %></li>
    <% end %>
  </ul>
<% end %>

<h1>Edit Item</h1>
<%= render "form" %>
<br/>
<!-- <%= form_tag item_path(@item), method: "delete" do %>
  <%= submit_tag "Delete Item" %>
<% end %> -->
